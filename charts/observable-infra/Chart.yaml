apiVersion: v2
name: observable-infra
description: A Helm chart for deploying infrastructure dependencies in development (kind).
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  # Official OpenFGA-maintained Helm chart. Keep this aligned with
  # docker-compose.yml's openfga/openfga image during dependency sweeps.
  - name: openfga
    version: "0.3.2"
    repository: "https://openfga.github.io/helm-charts"
