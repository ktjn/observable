[project]
name = "seed"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "clickhouse-connect>=0.7.19",
    "psycopg[binary]>=3.1.18",
    "tqdm>=4.66.4",
    "faker>=25.0.0",
    "pytest>=8.2.0",
]

[tool.uv]
package = false
