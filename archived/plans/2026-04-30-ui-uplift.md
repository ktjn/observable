# UI Uplift — Bloomberg Dense + Phosphor Green

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the current soft/rounded UI with a sharp, information-dense aesthetic: zero border-radius, ruled lines, IBM Plex condensed type, black-header tables, and a phosphor-green dark mode.

**Architecture:** All changes flow from CSS variables in `styles.css` (tokens + layout classes) down through Tailwind class strings in UI components. No new files are created — every change is a targeted edit. The font loads from Google Fonts via `index.html`.

**Tech Stack:** React 19, Tailwind CSS v4 (utility classes in TSX), CSS custom properties, IBM Plex Sans Condensed + IBM Plex Mono (Google Fonts)

---

## Files Modified

| File | What changes |
|------|-------------|
| `apps/frontend/index.html` | Add Google Fonts `<link>` preconnect + stylesheet |
| `apps/frontend/src/styles.css` | All CSS tokens, layout classes, global element styles |
| `apps/frontend/src/components/AppShell.tsx` | Remove `nav-icon` spans, update brand markup |
| `apps/frontend/src/components/ui/button.tsx` | Remove `rounded-md`, tighten sizing |
| `apps/frontend/src/components/ui/badge.tsx` | Remove `rounded-full`, change to outlined style |
| `apps/frontend/src/components/ui/input.tsx` | Remove `rounded-md` |
| `apps/frontend/src/components/ui/select.tsx` | Remove `rounded-md` |
| `apps/frontend/src/components/ui/tabs.tsx` | Remove `rounded-*` from list and tab |

---

## Task 1: Load IBM Plex Fonts

**Files:**
- Modify: `apps/frontend/index.html`

- [ ] **Step 1: Confirm tests are green before starting**

```bash
cd apps/frontend && npm test -- --run
```
Expected: all tests pass.

- [ ] **Step 2: Add font preconnect and stylesheet links**

Replace `apps/frontend/index.html` with:

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Observable</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;600&family=IBM+Plex+Sans+Condensed:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

- [ ] **Step 3: Commit**

```bash
git add apps/frontend/index.html
git commit -m "feat(ui): load IBM Plex Sans Condensed and IBM Plex Mono from Google Fonts"
```

---

## Task 2: Replace CSS Design Tokens

**Files:**
- Modify: `apps/frontend/src/styles.css` (`:root` and `[data-theme="dark"]` blocks only)

- [ ] **Step 1: Replace the light-mode `:root` block**

Find and replace the entire `:root { ... }` block (lines 6–33 in the current file) with:

```css
:root {
  color-scheme: light;
  --bg: #f2f2f2;
  --surface: #e8e8e8;
  --surface-subtle: #e4e4e4;
  --border: #cccccc;
  --border-strong: #999999;
  --text: #111111;
  --text-strong: #000000;
  --muted: #666666;
  --brand: #111111;
  --brand-strong: #000000;
  --brand-bg: #d8d8d8;
  --focus-ring: #555555;
  --surface-raised: #ffffff;
  --surface-inset: #ebebeb;
  --good: #007700;
  --good-bg: #f0f7f0;
  --warn: #a05000;
  --warn-bg: #f7f0e8;
  --bad: #bb0000;
  --bad-bg: #f7f0f0;
  --info-bg: #f0f0f7;
  --shadow-panel: none;
  --shadow-control: none;
  font-family: 'IBM Plex Sans Condensed', 'Arial Narrow', Arial, sans-serif;
  font-size: 11px;
}
```

- [ ] **Step 2: Replace the dark-mode block**

Find and replace the entire `:root[data-theme="dark"] { ... }` block with:

```css
:root[data-theme="dark"] {
  color-scheme: dark;
  --bg: #050f05;
  --surface: #070f07;
  --surface-subtle: #0a1f0a;
  --border: #0d2a0d;
  --border-strong: #1a5a1a;
  --text: #33ff33;
  --text-strong: #33ff33;
  --muted: #1a9a1a;
  --brand: #33ff33;
  --brand-strong: #33ff33;
  --brand-bg: #0a1f0a;
  --focus-ring: #33ff33;
  --surface-raised: #020a02;
  --surface-inset: #030a03;
  --good: #1a9a1a;
  --good-bg: #0a1f0a;
  --warn: #ffaa00;
  --warn-bg: #2a1a00;
  --bad: #ff5555;
  --bad-bg: #2a0a0a;
  --info-bg: #0a1f0a;
  --shadow-panel: none;
  --shadow-control: none;
}
```

- [ ] **Step 3: Run tests**

```bash
cd apps/frontend && npm test -- --run
```
Expected: all tests pass (token changes don't affect behavior tests).

- [ ] **Step 4: Commit**

```bash
git add apps/frontend/src/styles.css
git commit -m "feat(ui): replace design tokens — bloomberg dense light, phosphor green dark"
```

---

## Task 3: Rewrite Shell Layout CSS

**Files:**
- Modify: `apps/frontend/src/styles.css` (`.app-shell` through `.sidebar-footer` / `.segmented-control`)

Replace each class definition below. The goal: 140px→160px sidebar, no rounded corners, ruled-line nav, slimmer topbar.

- [ ] **Step 1: Update `.app-shell` grid width**

Find `.app-shell { ... }` and change `260px` to `160px`:

```css
.app-shell {
  min-height: 100vh;
  display: grid;
  grid-template-columns: 160px minmax(0, 1fr);
}
```

- [ ] **Step 2: Replace `.sidebar`**

```css
.sidebar {
  background: var(--surface-subtle);
  border-right: 1px solid var(--border-strong);
  display: flex;
  flex-direction: column;
  padding: 10px 0;
}
```

- [ ] **Step 3: Replace `.brand-lockup` and `.brand-mark`**

```css
.brand-lockup {
  padding: 0 8px 10px;
  border-bottom: 1px solid var(--border);
  margin-bottom: 6px;
}

.brand-mark {
  display: inline-block;
  background: var(--text);
  color: var(--bg);
  font-size: 9px;
  font-weight: 700;
  letter-spacing: 1.5px;
  text-transform: uppercase;
  padding: 2px 6px;
}
```

Remove `.brand-name` styles (the element remains in the DOM but will no longer be shown — it gets hidden in Task 5 via TSX changes).

- [ ] **Step 4: Replace `.nav-list`, `.nav-link`, `.nav-icon`**

```css
.nav-list {
  display: flex;
  flex-direction: column;
  gap: 0;
  flex: 1;
}

.nav-link,
.secondary-link {
  color: var(--text);
  text-decoration: none;
}

.nav-link {
  display: block;
  padding: 4px 10px;
  font-size: 11px;
  font-weight: 400;
  color: var(--muted);
  border-left: 2px solid transparent;
}

.nav-icon {
  display: none;
}

.nav-link:hover {
  background: var(--surface);
  color: var(--text);
  border-left-color: var(--border-strong);
}

.nav-link.active {
  background: var(--brand-bg);
  color: var(--text-strong);
  font-weight: 600;
  border-left-color: var(--brand);
  box-shadow: none;
}

.nav-link.active .nav-icon {
  background: transparent;
  color: inherit;
}
```

- [ ] **Step 5: Replace `.sidebar-footer` and `.segmented-control`**

```css
.sidebar-footer {
  margin-top: auto;
  padding: 8px 8px 0;
  border-top: 1px solid var(--border);
  display: grid;
  gap: 4px;
}

.segmented-control {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  border: 1px solid var(--border-strong);
  overflow: hidden;
}

.segment {
  border: 0;
  border-right: 1px solid var(--border-strong);
  background: var(--surface);
  color: var(--muted);
  min-height: 28px;
  cursor: pointer;
  font-size: 10px;
}

.segment:last-child {
  border-right: 0;
}

.segment.active {
  background: var(--brand);
  color: var(--bg);
}
```

- [ ] **Step 6: Replace `.workspace`, `.topbar`, `.topbar-title`, `.topbar-controls`/`.toolbar-row`**

```css
.workspace {
  min-width: 0;
  display: grid;
  grid-template-rows: auto 1fr;
}

.topbar {
  min-height: 36px;
  background: var(--surface);
  border-bottom: 2px solid var(--text);
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
  padding: 4px 16px;
}

.topbar-title {
  font-size: 11px;
  font-weight: 700;
  letter-spacing: 0.5px;
  text-transform: uppercase;
}

.topbar-controls,
.toolbar-row {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 6px;
}

.context-pill,
.secondary-link {
  border: 1px solid var(--border-strong);
  padding: 1px 6px;
  background: var(--surface-raised);
  color: var(--muted);
  font-size: 10px;
  font-weight: 400;
}

.secondary-link:hover {
  color: var(--text);
  border-color: var(--text);
}
```

- [ ] **Step 7: Replace `.content-shell` and `.page-stack`**

```css
.content-shell {
  min-width: 0;
  padding: 16px;
}

.page-stack {
  display: grid;
  gap: 12px;
}
```

- [ ] **Step 8: Run tests**

```bash
cd apps/frontend && npm test -- --run
```
Expected: all tests pass.

- [ ] **Step 9: Commit**

```bash
git add apps/frontend/src/styles.css
git commit -m "feat(ui): restyle shell — sharp sidebar, slim topbar, ruled-line nav"
```

---

## Task 4: Rewrite Component CSS Classes

**Files:**
- Modify: `apps/frontend/src/styles.css` (panels, inputs, tables, facets, misc)

- [ ] **Step 1: Replace global element styles — `h1`, `button`/`input`/`select`, `table`/`th`/`td`**

Find the existing `h1`, `button, input, select`, `table`, `th, td`, `th` blocks and replace:

```css
button,
input,
select {
  font: inherit;
}

h1 {
  margin: 4px 0 0;
  font-size: 20px;
  font-weight: 700;
  letter-spacing: 0.5px;
}

table {
  width: 100%;
  border-collapse: collapse;
}

thead tr {
  background: var(--brand);
  color: var(--bg);
}

th,
td {
  padding: 3px 6px;
  border-bottom: 1px solid var(--border);
  text-align: left;
  white-space: nowrap;
}

th {
  font-size: 9px;
  font-weight: 400;
  letter-spacing: 0.5px;
  text-transform: uppercase;
}

tbody tr:nth-child(even) td {
  background: var(--surface-inset);
}

.strong-cell {
  font-weight: 700;
}
```

- [ ] **Step 2: Replace `.search-input`, `.select-input`**

```css
.search-input,
.select-input {
  min-height: 28px;
  border: 1px solid var(--border-strong);
  background: var(--surface-raised);
  color: var(--text);
  padding: 0 8px;
  font-family: 'IBM Plex Mono', 'Courier New', monospace;
  font-size: 11px;
}

.search-input {
  width: min(360px, 100%);
}
```

- [ ] **Step 3: Replace `.modern-panel`, `.modern-panel-header`, `.modern-panel-body`**

```css
.modern-panel {
  background: var(--surface-raised);
  border: 1px solid var(--border-strong);
}

.modern-panel-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 12px;
  padding: 4px 8px;
  border-bottom: 1px solid var(--border-strong);
  background: var(--surface);
}

.modern-panel-body {
  padding: 10px;
}

.modern-toolbar {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 6px;
}
```

- [ ] **Step 4: Replace `.modern-signal-tabs`, `.modern-signal-tab`**

```css
.modern-signal-tabs {
  display: flex;
  flex-wrap: wrap;
  gap: 0;
  padding: 0;
  border-bottom: 1px solid var(--border-strong);
  background: var(--surface);
}

.modern-signal-tab {
  min-height: 30px;
  padding: 4px 12px;
  color: var(--muted);
  text-decoration: none;
  font-size: 11px;
  font-weight: 400;
  border-right: 1px solid var(--border);
  border-bottom: 2px solid transparent;
}

.modern-signal-tab:hover,
.modern-signal-tab.active {
  background: var(--surface-raised);
  color: var(--text);
  border-bottom-color: var(--brand);
  box-shadow: none;
}
```

- [ ] **Step 5: Replace `.modern-table-row`, `.page-header`**

```css
.modern-table-row:hover td {
  background: var(--surface-subtle);
}

.page-header {
  display: flex;
  align-items: end;
  justify-content: space-between;
  gap: 16px;
}
```

- [ ] **Step 6: Replace `.detail-grid`, `.definition-grid`**

```css
.detail-grid {
  display: grid;
  grid-template-columns: minmax(0, 1.2fr) minmax(280px, 0.8fr);
  gap: 8px;
}

.definition-grid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 8px;
  margin: 0;
}

.definition-grid div {
  background: var(--surface-subtle);
  border: 1px solid var(--border);
  padding: 8px;
}

.definition-grid dt {
  color: var(--muted);
  font-size: 9px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.definition-grid dd {
  margin: 4px 0 0;
  font-weight: 700;
}
```

- [ ] **Step 7: Replace `.deployment-timeline-tooltip`, `.trace-explorer-page`, `.signal-empty`, `.facet-*`**

```css
.deployment-timeline-tooltip {
  position: absolute;
  top: 100%;
  left: 16px;
  z-index: 10;
  pointer-events: none;
  white-space: nowrap;
  border: 1px solid var(--border-strong);
  background: var(--surface-raised);
  color: var(--text);
  padding: 4px 8px;
  font-size: 10px;
  box-shadow: none;
}

.deployment-timeline-commit {
  font-family: 'IBM Plex Mono', ui-monospace, monospace;
}

.trace-explorer-page {
  background: var(--surface);
  border: 1px solid var(--border-strong);
  padding: 16px;
}

.signal-empty {
  min-height: 80px;
  display: grid;
  place-items: center;
  padding: 16px;
  color: var(--muted);
  text-align: center;
}

.facet-sidebar {
  width: 220px;
  flex-shrink: 0;
  padding-right: 16px;
  border-right: 1px solid var(--border);
}

.facet-group {
  margin-bottom: 16px;
}

.facet-title {
  color: var(--muted);
  font-size: 9px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  margin-bottom: 6px;
}

.facet-list {
  list-style: none;
  padding: 0;
  margin: 0;
  display: grid;
  gap: 2px;
}

.facet-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 2px 4px;
  font-size: 11px;
  cursor: pointer;
  color: var(--text);
}

.facet-item:hover {
  background: var(--surface-subtle);
}

.facet-value {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  margin-right: 8px;
}

.facet-count {
  color: var(--muted);
  font-size: 10px;
  flex-shrink: 0;
  font-family: 'IBM Plex Mono', monospace;
}
```

- [ ] **Step 8: Update `.setup-actions` and `field-label`/`brand-context`/`metric-label`**

```css
.brand-context,
.field-label,
.metric-label {
  color: var(--muted);
  font-size: 9px;
}

.field-label {
  text-transform: uppercase;
  letter-spacing: 0.5px;
  font-weight: 700;
}

.setup-actions {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 8px;
  margin-top: 12px;
}
```

- [ ] **Step 9: Run tests**

```bash
cd apps/frontend && npm test -- --run
```
Expected: all tests pass.

- [ ] **Step 10: Commit**

```bash
git add apps/frontend/src/styles.css
git commit -m "feat(ui): zero border-radius, black table headers, dense panels and facets"
```

---

## Task 5: Update AppShell Navigation

**Files:**
- Modify: `apps/frontend/src/components/AppShell.tsx`

The icon boxes (`<span className="nav-icon">`) are hidden by CSS in Task 3, but we should remove them from the DOM entirely and simplify the brand lockup.

- [ ] **Step 1: Rewrite `AppShell.tsx`**

Replace the entire file with:

```tsx
import { Link, Outlet } from "@tanstack/react-router";
import { useTheme, type ThemePreference } from "../lib/theme";

const navItems = [
  { label: "Setup", to: "/setup" },
  { label: "Services", to: "/services" },
  { label: "Traces", to: "/traces" },
  { label: "Logs", to: "/logs" },
  { label: "Infrastructure", to: "/infrastructure" },
  { label: "Service Overview", to: "/service-overview" },
  { label: "Dashboards", to: "/dashboards" },
  { label: "Alerts & SLOs", to: "/alerts" },
  { label: "Admin", to: "/admin" },
] as const;

const themeOptions: { label: string; value: ThemePreference }[] = [
  { label: "Light", value: "light" },
  { label: "Dark", value: "dark" },
  { label: "Sys", value: "system" },
];

export function AppShell() {
  const { preference, setPreference } = useTheme();

  return (
    <div className="app-shell">
      <aside className="sidebar" aria-label="Primary navigation">
        <div className="brand-lockup">
          <div className="brand-mark" aria-label="Observable">OBSERVABLE</div>
        </div>

        <nav className="nav-list">
          {navItems.map((item) => (
            <Link
              key={item.to}
              to={item.to}
              className="nav-link"
              activeProps={{ className: "nav-link active" }}
            >
              {item.label}
            </Link>
          ))}
        </nav>

        <div className="sidebar-footer">
          <div className="field-label">Theme</div>
          <div className="segmented-control" role="radiogroup" aria-label="Theme preference">
            {themeOptions.map((option) => (
              <button
                key={option.value}
                type="button"
                role="radio"
                aria-checked={preference === option.value}
                className={preference === option.value ? "segment active" : "segment"}
                onClick={() => setPreference(option.value)}
              >
                {option.label}
              </button>
            ))}
          </div>
        </div>
      </aside>

      <div className="workspace">
        <header className="topbar">
          <div className="topbar-title">Platform — dev</div>
          <div className="topbar-controls" aria-label="Global context">
            <span className="context-pill">UTC</span>
            <span className="context-pill">Last 1h</span>
            <Link to="/traces" className="secondary-link">Traces</Link>
            <Link to="/logs" className="secondary-link">Logs</Link>
          </div>
        </header>

        <main className="content-shell">
          <Outlet />
        </main>
      </div>
    </div>
  );
}
```

Note: "Admin / Fleet / Billing" shortened to "Admin" for sidebar width. "System" shortened to "Sys" in theme switcher for segment width.

- [ ] **Step 2: Run tests**

```bash
cd apps/frontend && npm test -- --run
```
Expected: all tests pass.

- [ ] **Step 3: Commit**

```bash
git add apps/frontend/src/components/AppShell.tsx
git commit -m "feat(ui): remove nav icon boxes, slim brand lockup in AppShell"
```

---

## Task 6: Update Button Component

**Files:**
- Modify: `apps/frontend/src/components/ui/button.tsx`

Remove `rounded-md`. Shrink min-height. Update primary variant to use inverted brand colors (in light mode `--brand` is now black, so primary button is black/white — correct).

- [ ] **Step 1: Rewrite `button.tsx`**

```tsx
import { forwardRef } from "react";
import type { ButtonHTMLAttributes } from "react";
import { cn } from "./cn";

type ButtonVariant = "primary" | "secondary" | "destructive" | "ghost";

export interface ButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: ButtonVariant;
}

const variantClasses: Record<ButtonVariant, string> = {
  primary:
    "bg-[var(--brand)] text-[var(--bg)] hover:bg-[var(--brand-strong)] disabled:bg-[var(--surface-subtle)] disabled:text-[var(--muted)]",
  secondary:
    "border border-[var(--border-strong)] bg-[var(--surface)] text-[var(--text)] hover:bg-[var(--surface-subtle)] disabled:text-[var(--muted)]",
  destructive:
    "bg-[var(--bad)] text-white hover:opacity-90 disabled:bg-[var(--surface-subtle)] disabled:text-[var(--muted)]",
  ghost:
    "bg-transparent text-[var(--muted)] hover:bg-[var(--surface-subtle)] hover:text-[var(--text)] disabled:text-[var(--muted)]",
};

export const Button = forwardRef<HTMLButtonElement, ButtonProps>(function Button(
  { className, type = "button", variant = "primary", ...props },
  ref
) {
  return (
    <button
      ref={ref}
      type={type}
      className={cn(
        "inline-flex min-h-7 items-center justify-center px-3 text-[11px] font-semibold outline-none transition-colors",
        "focus-visible:ring-1 focus-visible:ring-[var(--focus-ring)] focus-visible:ring-offset-1 focus-visible:ring-offset-[var(--bg)]",
        "disabled:cursor-not-allowed",
        variantClasses[variant],
        className
      )}
      {...props}
    />
  );
});
```

- [ ] **Step 2: Run tests**

```bash
cd apps/frontend && npm test -- --run
```
Expected: all tests pass (tests check behavior, not class names).

- [ ] **Step 3: Commit**

```bash
git add apps/frontend/src/components/ui/button.tsx
git commit -m "feat(ui): remove rounded corners from Button, shrink to dense height"
```

---

## Task 7: Update Badge Component

**Files:**
- Modify: `apps/frontend/src/components/ui/badge.tsx`

Switch from filled pill to outlined badge. Remove `rounded-full`.

- [ ] **Step 1: Rewrite `badge.tsx`**

```tsx
import type { HTMLAttributes, ReactNode } from "react";
import { cn } from "./cn";

type BadgeTone = "good" | "warn" | "bad" | "info" | "neutral";

export interface BadgeProps extends HTMLAttributes<HTMLSpanElement> {
  tone?: BadgeTone;
  children: ReactNode;
}

const toneClasses: Record<BadgeTone, string> = {
  good: "border border-[var(--good)] text-[var(--good)]",
  warn: "border border-[var(--warn)] text-[var(--warn)]",
  bad: "border border-[var(--bad)] text-[var(--bad)]",
  info: "border border-[var(--brand)] text-[var(--brand)]",
  neutral: "border border-[var(--border-strong)] text-[var(--muted)]",
};

export function Badge({ tone = "neutral", className, children, ...props }: BadgeProps) {
  return (
    <span
      role="status"
      className={cn(
        "inline-flex min-h-5 items-center px-1.5 text-[9px] font-bold uppercase tracking-wide",
        toneClasses[tone],
        className
      )}
      {...props}
    >
      {children}
    </span>
  );
}

type HealthState = "healthy" | "watch" | "breach" | "unknown";

const dotClasses: Record<HealthState, string> = {
  healthy: "bg-[var(--good)]",
  watch: "bg-[var(--warn)]",
  breach: "bg-[var(--bad)]",
  unknown: "bg-[var(--muted)]",
};

export function HealthDot({ state }: { state: HealthState }) {
  return (
    <span
      role="img"
      aria-label={state}
      className={cn("inline-block h-2 w-2 flex-shrink-0", dotClasses[state])}
    />
  );
}
```

- [ ] **Step 2: Run tests**

```bash
cd apps/frontend && npm test -- --run
```
Expected: all tests pass.

- [ ] **Step 3: Commit**

```bash
git add apps/frontend/src/components/ui/badge.tsx
git commit -m "feat(ui): switch Badge to outlined style, remove rounded-full"
```

---

## Task 8: Update Panel Component

**Files:**
- Modify: `apps/frontend/src/components/ui/panel.tsx`

The panel title uses `text-lg` (18px Tailwind), which is too large for the dense aesthetic. Shrink to 13px and reduce font weight.

- [ ] **Step 1: Rewrite `panel.tsx`**

```tsx
import type { HTMLAttributes, ReactNode } from "react";
import { cn } from "./cn";

export interface PanelProps extends HTMLAttributes<HTMLElement> {
  title?: string;
  eyebrow?: string;
  actions?: ReactNode;
  children: ReactNode;
}

export function Panel({
  title,
  eyebrow,
  actions,
  children,
  className,
  ...props
}: PanelProps) {
  return (
    <section className={cn("modern-panel overflow-hidden", className)} {...props}>
      {(title || eyebrow || actions) && (
        <div className="modern-panel-header">
          <div>
            {eyebrow && <div className="field-label">{eyebrow}</div>}
            {title && <h2 className="m-0 text-[13px] font-semibold text-[var(--text-strong)]">{title}</h2>}
          </div>
          {actions && <div className="modern-toolbar">{actions}</div>}
        </div>
      )}
      <div className="modern-panel-body">{children}</div>
    </section>
  );
}
```

- [ ] **Step 2: Run tests**

```bash
cd apps/frontend && npm test -- --run
```
Expected: all tests pass.

- [ ] **Step 3: Commit**

```bash
git add apps/frontend/src/components/ui/panel.tsx
git commit -m "feat(ui): shrink Panel title to 13px for dense aesthetic"
```

---

## Task 9: Update Input and Select Components  

**Files:**
- Modify: `apps/frontend/src/components/ui/input.tsx`
- Modify: `apps/frontend/src/components/ui/select.tsx`

Remove `rounded-md`. Use monospace font for input (matches query/filter aesthetic). Tighten min-height.

- [ ] **Step 1: Rewrite `input.tsx`**

```tsx
import { forwardRef } from "react";
import type { InputHTMLAttributes } from "react";
import { cn } from "./cn";

export type InputProps = InputHTMLAttributes<HTMLInputElement>;

export const Input = forwardRef<HTMLInputElement, InputProps>(function Input(
  { className, type = "text", ...props },
  ref
) {
  return (
    <input
      ref={ref}
      type={type}
      className={cn(
        "flex min-h-7 w-full border border-[var(--border-strong)] bg-[var(--surface-raised)] px-2 font-[family-name:'IBM_Plex_Mono',monospace] text-[11px] text-[var(--text)] outline-none transition-colors",
        "placeholder:text-[var(--muted)]",
        "focus-visible:ring-1 focus-visible:ring-[var(--focus-ring)] focus-visible:ring-offset-1 focus-visible:ring-offset-[var(--bg)]",
        "disabled:cursor-not-allowed disabled:opacity-60",
        className
      )}
      {...props}
    />
  );
});
```

- [ ] **Step 2: Rewrite `select.tsx`**

```tsx
import { forwardRef } from "react";
import type { OptionHTMLAttributes, SelectHTMLAttributes } from "react";
import { cn } from "./cn";

export type SelectProps = SelectHTMLAttributes<HTMLSelectElement>;

export type SelectOptionProps = OptionHTMLAttributes<HTMLOptionElement>;

const triggerClasses =
  "flex min-h-7 w-full border border-[var(--border-strong)] bg-[var(--surface-raised)] px-2 text-[11px] text-[var(--text)] outline-none transition-colors focus-visible:ring-1 focus-visible:ring-[var(--focus-ring)] focus-visible:ring-offset-1 focus-visible:ring-offset-[var(--bg)] disabled:cursor-not-allowed disabled:opacity-60";

export const Select = forwardRef<HTMLSelectElement, SelectProps>(function Select(
  { className, children, ...props },
  ref
) {
  return (
    <select ref={ref} className={cn(triggerClasses, className)} {...props}>
      {children}
    </select>
  );
});

export function SelectOption(props: SelectOptionProps) {
  return <option {...props} />;
}
```

- [ ] **Step 3: Run tests**

```bash
cd apps/frontend && npm test -- --run
```
Expected: all tests pass.

- [ ] **Step 4: Commit**

```bash
git add apps/frontend/src/components/ui/input.tsx apps/frontend/src/components/ui/select.tsx
git commit -m "feat(ui): remove rounded corners from Input and Select, use monospace for Input"
```

---

## Task 10: Update Tabs Component

**Files:**
- Modify: `apps/frontend/src/components/ui/tabs.tsx`

Remove `rounded-lg` from the list container and `rounded-md` from individual tabs. Switch from pill tabs to an underline-active style.

- [ ] **Step 1: Rewrite `tabs.tsx`**

```tsx
import { Tabs as BaseTabs } from "@base-ui/react/tabs";
import { forwardRef } from "react";
import type { ComponentPropsWithoutRef, ElementRef } from "react";
import { cn } from "./cn";

function mergeClassName<State>(
  baseClassName: string,
  className?: string | ((state: State) => string | undefined)
) {
  if (typeof className === "function") {
    return (state: State) => cn(baseClassName, className(state));
  }

  return cn(baseClassName, className);
}

export function TabsRoot(props: BaseTabs.Root.Props) {
  return <BaseTabs.Root {...props} />;
}

export const TabsList = forwardRef<
  ElementRef<typeof BaseTabs.List>,
  ComponentPropsWithoutRef<typeof BaseTabs.List>
>(function TabsList({ className, ...props }, ref) {
  return (
    <BaseTabs.List
      ref={ref}
      className={mergeClassName(
        "relative inline-flex min-h-8 items-end gap-0 border-b border-[var(--border-strong)] bg-transparent",
        className
      )}
      {...props}
    />
  );
});

export const TabsTab = forwardRef<
  ElementRef<typeof BaseTabs.Tab>,
  ComponentPropsWithoutRef<typeof BaseTabs.Tab>
>(function TabsTab({ className, ...props }, ref) {
  return (
    <BaseTabs.Tab
      ref={ref}
      className={mergeClassName(
        cn(
          "inline-flex min-h-7 items-center justify-center border-b-2 border-transparent px-3 text-[11px] font-medium text-[var(--muted)] outline-none transition-colors",
          "data-[selected]:border-[var(--brand)] data-[selected]:text-[var(--text)] data-[selected]:font-semibold",
          "hover:text-[var(--text)]",
          "focus-visible:ring-1 focus-visible:ring-[var(--focus-ring)] focus-visible:ring-offset-1 focus-visible:ring-offset-[var(--bg)]"
        ),
        className
      )}
      {...props}
    />
  );
});

export const TabsPanel = forwardRef<
  ElementRef<typeof BaseTabs.Panel>,
  ComponentPropsWithoutRef<typeof BaseTabs.Panel>
>(function TabsPanel({ className, ...props }, ref) {
  return (
    <BaseTabs.Panel
      ref={ref}
      className={mergeClassName("outline-none", className)}
      {...props}
    />
  );
});

export const Tabs = {
  Root: TabsRoot,
  List: TabsList,
  Tab: TabsTab,
  Panel: TabsPanel,
};
```

- [ ] **Step 2: Run tests**

```bash
cd apps/frontend && npm test -- --run
```
Expected: all tests pass.

- [ ] **Step 3: Commit**

```bash
git add apps/frontend/src/components/ui/tabs.tsx
git commit -m "feat(ui): replace pill tabs with underline-active tabs, remove rounded corners"
```

---

## Task 11: Visual Verification

**No file changes — manual check only.**

- [ ] **Step 1: Start dev server**

```bash
cd apps/frontend && npm run dev
```
Open `http://localhost:5173` in a browser.

- [ ] **Step 2: Light mode checklist**

Switch to **Light** theme and verify:
- [ ] Sidebar is narrower (~160px), text-only nav, black left-bar on active item
- [ ] Logo shows as a black pill with "OBSERVABLE" white text
- [ ] Topbar is slim (~36px) with bold uppercase title, 2px black bottom border
- [ ] Table headers are black background with white text
- [ ] Table rows zebra-stripe (alternating light gray)
- [ ] No rounded corners visible anywhere (panels, buttons, inputs, badges, tabs)
- [ ] IBM Plex Sans Condensed is loading (check Network tab for fonts.gstatic.com requests)
- [ ] Badges show as outlined (border only, no fill)
- [ ] Buttons: primary = black bg/white text; secondary = border only

- [ ] **Step 3: Dark mode checklist**

Switch to **Dark** theme and verify:
- [ ] Background is very dark green (#050f05)
- [ ] Primary text is bright phosphor green (#33ff33)
- [ ] Active nav item has bright green left-bar indicator
- [ ] Logo shows as a green-outlined box with "OBSERVABLE" text
- [ ] Table header row is dark green background (#0a1f0a)
- [ ] Error badges/text are red (#ff5555), warnings are amber (#ffaa00)
- [ ] No rounded corners
- [ ] Segmented control: active segment is bright green bg + dark text

- [ ] **Step 4: Check key pages**

Visit each page and confirm no visual regressions:
- `/logs` — log table, search input, facet sidebar
- `/services` — service list table, badges
- `/traces` — trace explorer
- `/setup` — form inputs, buttons
- `/alerts` — alert list

- [ ] **Step 5: Typecheck and lint**

```bash
cd apps/frontend && npm run typecheck && npm run lint
```
Expected: no errors.

- [ ] **Step 6: Final test run**

```bash
cd apps/frontend && npm test -- --run
```
Expected: all tests pass.
